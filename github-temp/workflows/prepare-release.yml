name: Prepare Release

on:
  workflow_dispatch:
    inputs:
      release_version:
        description: "Version to release (e.g. 2.0.0 or 2.0.0-rc1)"
        required: true
      development_version:
        description: "Optional: next dev version to continue on (e.g. 2.0.1-dev). Leave blank to skip."
        required: false

# No default permissions; each job below declares exactly what it needs.
permissions: {}

jobs:
  # Runs the version bump and build. No write access, so a compromised
  # dependency's postinstall script has nothing to push during `yarn install`.
  build:
    runs-on: ubuntu-x64
    permissions:
      contents: read
      id-token: write # for the Artifactory OIDC exchange below
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # 7.0.1

      - name: Artifactory OIDC Auth
        uses: ./.github/actions/artifactory-oidc
        with:
          artifactory_url: ${{ vars.ARTIFACTORY_URL }}

      - run: corepack enable

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # 7.0.0
        with:
          node-version: "22.13"
          cache: yarn

      - name: Parse version number for validity
        env:
          RELEASE_VERSION: ${{ inputs.release_version }}
        run: |
          set -euo pipefail

          IS_RELEASE_CANDIDATE=$(node ./scripts/classify-version-tag.js getIsReleaseCandidate "${RELEASE_VERSION}")
          echo "::notice::${RELEASE_VERSION} - is release candidate: ${IS_RELEASE_CANDIDATE}"

          if [ "${IS_RELEASE_CANDIDATE}" = 'false' ]; then
            RELEASE_CHANNEL=$(node ./scripts/classify-version-tag.js getReleaseChannel "${RELEASE_VERSION}")
            echo "::notice::${RELEASE_VERSION} - release channel: ${RELEASE_CHANNEL}"

            CHECKED_LATEST_NPM_DIST_TAG=$(node ./scripts/classify-version-tag.js getNpmDistTag "${RELEASE_VERSION}" "true")
            echo "::notice::${RELEASE_VERSION} - npm dist-tag if latest checkbox is checked: ${CHECKED_LATEST_NPM_DIST_TAG}"

            UNCHECKED_LATEST_NPM_DIST_TAG=$(node ./scripts/classify-version-tag.js getNpmDistTag "${RELEASE_VERSION}" "false")
            echo "::notice::${RELEASE_VERSION} - npm dist-tag if latest checkbox is unchecked: ${UNCHECKED_LATEST_NPM_DIST_TAG}"
          fi

      - run: yarn install --immutable

      - name: Bump package.json version
        env:
          RELEASE_VERSION: ${{ inputs.release_version }}
        run: npm version --no-git-tag-version "${RELEASE_VERSION}"

      - name: Stamp constants files
        env:
          RELEASE_VERSION: ${{ inputs.release_version }}
        run: node ./scripts/substitute-constants-version.js "${RELEASE_VERSION}"

      - name: Build SDK artifacts
        run: yarn run build:docs

      # VBLOCKS-7002 - Consider going further and checking the build artifacts
      # for integrity. Also consider a mechanism to synchronize the checked
      # artifacts here with the list of actually generated artifacts.
      - name: Verify the release tree was built
        run: |
          set -euo pipefail

          for RELEASE_FILE in \
            package.json \
            constants/constants.src \
            src/constants.ts \
            ios/TwilioVoiceReactNativeConstants.h \
            android/src/main/java/com/twiliovoicereactnative/CommonConstants.java
          do
            if [ ! -s "$RELEASE_FILE" ]; then
              echo "::error::${RELEASE_FILE} is missing or empty after the build."
              exit 1
            fi
          done

          for RELEASE_DIRECTORY in docs/api lib; do
            if [ ! -d "$RELEASE_DIRECTORY" ] ||
               [ -z "$(ls -A "$RELEASE_DIRECTORY")" ]; then
              echo "::error::${RELEASE_DIRECTORY}/ is missing or empty after the build."
              exit 1
            fi
          done

      - name: Upload release tree
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # 7.0.1
        with:
          name: release-tree
          if-no-files-found: error
          path: |
            package.json
            constants/constants.src
            docs/api/
            lib/
            src/constants.ts
            ios/TwilioVoiceReactNativeConstants.h
            android/src/main/java/com/twiliovoicereactnative/CommonConstants.java

      # The runner group is self-hosted, so $HOME can outlive the job.
      - name: Remove Artifactory credentials
        if: always()
        run: rm -f ~/.npmrc ~/.yarnrc.yml

  # Never runs dependency code - just git commands. Pushes a new branch
  # (not `main`) for a human to open the PR manually.
  create-release-branch:
    needs: build
    runs-on: ubuntu-x64
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # 7.0.1

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # 8.0.1
        with:
          name: release-tree

      - name: Configure git identity
        run: |
          set -euo pipefail

          git config user.name "twilio-vblocks-ci"
          git config user.email "svc.vblocks-ci@twilio.com"

      # Only needed for the development-version continuation step below,
      # which runs npm/node. Skipped when there's no continuation to build.
      - name: Set up Node
        if: inputs.development_version != ''
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # 7.0.0
        with:
          node-version: "22.13"

      - name: Commit release bump
        env:
          RELEASE_VERSION: ${{ inputs.release_version }}
        run: |
          set -euo pipefail

          git checkout -b "release/${RELEASE_VERSION}"
          git add package.json
          git add constants/constants.src
          git add -f docs/api
          git add -f lib
          git add -f src/constants.ts
          git add -f ios/TwilioVoiceReactNativeConstants.h
          git add -f android/src/main/java/com/twiliovoicereactnative/CommonConstants.java
          git commit --no-verify -m "chore: prepare release ${RELEASE_VERSION}"

          # Consider uncommenting if we can have GHA automatically open the PR.
          # echo "RELEASE_COMMIT_SHA=$(git rev-parse HEAD)" >> "$GITHUB_ENV"

          echo "::notice::Release commit hash: $(git rev-parse HEAD)"

      # Committed as a second commit on the same branch rather than a
      # separate PR. Merging must use "Create a merge commit" - squash or
      # rebase would fold this into the release commit, and the GitHub
      # Release needs to target the release commit specifically, not the
      # branch tip.
      - name: Commit development-version continuation
        if: inputs.development_version != ''
        env:
          DEVELOPMENT_VERSION: ${{ inputs.development_version }}
        run: |
          set -euo pipefail

          npm version --no-git-tag-version "${DEVELOPMENT_VERSION}"
          node ./scripts/substitute-constants-version.js "${DEVELOPMENT_VERSION}"
          git rm -r --ignore-unmatch \
            docs/api \
            lib \
            src/constants.ts \
            ios/TwilioVoiceReactNativeConstants.h \
            android/src/main/java/com/twiliovoicereactnative/CommonConstants.java
          git add -A
          git commit --no-verify -m "chore: continue development on ${DEVELOPMENT_VERSION}"

      # Known gap: re-dispatching this workflow for a version whose branch was
      # already pushed rebuilds everything and then fails here, because the
      # fresh local branch has no shared history with the remote one. Loud and
      # recoverable, but only after the whole build has run again, and recovery
      # means deleting the remote branch by hand.
      #
      # Deliberately not a force-push. That would discard whatever is on the
      # existing branch, including a branch someone has already opened a PR
      # from. If this becomes annoying, check for the remote branch before the
      # build rather than relaxing the push.
      - name: Push release branch
        env:
          RELEASE_VERSION: ${{ inputs.release_version }}
        run: git push origin "release/${RELEASE_VERSION}"

      # Disabled org-wide for GitHub Actions bots. A human opens the PR
      # manually from this branch to main instead. Kept here, commented out,
      # in case that permission is ever granted. Also hardcodes `--base
      # main` below - releases aren't always cut from main, so this would
      # need to become a parameter before re-enabling.
      # - name: Open pull request
      #   env:
      #     GH_TOKEN: ${{ github.token }}
      #     RELEASE_VERSION: ${{ inputs.release_version }}
      #   run: |
      #     gh pr create \
      #       --title "Release ${RELEASE_VERSION}" \
      #       --body "Automated release preparation for ${RELEASE_VERSION}.
      #
      #     **Merge via \"Create a merge commit\"** - do not squash or rebase.
      #
      #     When drafting the GitHub Release afterward, target this commit specifically:
      #     \`${RELEASE_COMMIT_SHA}\`
      #
      #     (Not \`main\`'s tip, if a development-version continuation commit is also in this PR - that commit lands on top of this one and \`main\`'s tip would reflect the dev-continuation state, not this release.)" \
      #       --base main \
      #       --head "release/${RELEASE_VERSION}"
