steps:
  - label: ":white_check_mark: Smoke test"
    command: 'echo "BUILDKITE OK — config was pulled and executed successfully"'
  - label: "what is the registry"
    command: 'npm config get registry'
